version 1.0
made the bot itself very simple randomly picks a song daily

------------------------------------------------------------------------------------------------------------------------------------------
version 1.1 
  cleaned up the bots code removing traces of it orginally being a webhook
  added commands to schedule songs 
  added status stuff
  add commands to only allow certain roles to only use the commands
